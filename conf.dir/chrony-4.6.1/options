JGB_BUILD_CONFIGURE_OPTIONS_HOST=no
CONFIGURE_OPTIONS="--sysconfdir=$JGB_BUILD_DESTDIR --chronyrundir=$JGB_BUILD_DESTDIR --localstatedir=$JGB_BUILD_DESTDIR"
if is_cross_compile ; then
  export CC=${JGB_BUILD_HOST}-gcc
fi
