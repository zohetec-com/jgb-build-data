CFLAGS_OPTIONS="-UDEBUG"
if [ -f /etc/lsb-release ] ; then
  . /etc/lsb-release
  if [ "$DISTRIB_RELEASE" == "20.04" ] ; then
    CMAKE_OPTIONS="-DOPENSSL_ROOT_DIR=${JGB_BUILD_DESTDIR}/lib64"
  fi
fi
