if is_cross_compile ; then
  if [ "$JGB_BUILD_HOST_ARCH" == aarch64 ] ; then
  	MAKE_OPTIONS="PREFIX=$JGB_BUILD_DESTDIR CC=${JGB_BUILD_HOST}-gcc AR=${JGB_BUILD_HOST}-ar"
	MAKE_INSTALL_OPTIONS="DESTDIR= PREFIX=$JGB_BUILD_DESTDIR"
  fi
fi

