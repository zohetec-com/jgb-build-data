if [ $JGB_BUILD_BUILD_ARCH != $JGB_BUILD_HOST_ARCH ] ; then
  if [ "$JGB_BUILD_HOST_ARCH" == aarch64 ] ; then
  	JGB_BUILD_MAKE_OPTIONS="PREFIX=$JGB_BUILD_DESTDIR CC=${JGB_BUILD_HOST}-gcc AR=${JGB_BUILD_HOST}-ar"
	JGB_BUILD_MAKE_INSTALL_OPTIONS="DESTDIR= PREFIX=$JGB_BUILD_DESTDIR"
  fi
fi

